# data/objectives

Objective database materials belong here.

This layer should eventually hold the objective schema, templates, and working objective database before any route skeleton tries to place objectives.

Start with `schema.md`, then use `taxonomy.md` to decide what kinds of rows should exist. Use `objectives.template.csv` as the header source for `objectives.csv`. Keep `objectives.csv` header-only until gameplay research begins.
